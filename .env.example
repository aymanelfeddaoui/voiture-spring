# Copier vers .env : copy .env.example .env  (Windows) ou cp .env.example .env
# Ne jamais committer le fichier .env (clés réelles).

ANTHROPIC_API_KEY=
JWT_SECRET=change-me-to-a-long-random-secret-at-least-256-bits
MARIADB_ROOT_PASSWORD=root
MARIADB_DATABASE=compagnie
MARIADB_USER=voiture
MARIADB_PASSWORD=voiture
